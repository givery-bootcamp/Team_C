# generated by octocov init
coverage:
  if: true
codeToTestRatio:
  if: true
  code:
    - '**/*.go'
    - '!**/*_test.go'
  test:
    - '**/*_test.go'
testExecutionTime:
  if: true
diff:
  if: is_pull_request
  datastores:
    - artifact://${GITHUB_REPOSITORY}/e2e
report:
  if: true
  datastores:
    - artifact://${GITHUB_REPOSITORY}/e2e
comment:
  if: is_pull_request
  hideFooterLink: true
  title: '【E2E Test】Coverage Report'
