desc: Health Check
runners:
  req: http://localhost:9000
if: included
steps:
  - desc: "Health Checkが返ってくる"
    req:
      /:
        get:
          body: null
    test: |
      current.res.status == 200
